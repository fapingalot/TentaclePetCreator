{{!-- Selected icon !--}}
<i 
  class="selected-icon material-icons {{if isSelected "visible"}} {{unless canChange "disabled"}}"
>
  check_circle
</i>

{{!-- Clickable area !--}}
<div {{action "toggleSelected"}}
  class="mdc-card__primary-action"
>
  {{!-- Title !--}}
  <div class="tentacle-title-container mdc-card__actions">
    <h2 class="tentacle-title">{{title}}</h2>
  </div>
  
  {{!-- Picture !--}}
  <div
    class="mdc-card__media tentacle-image" 
    style={{imageStyle}}
  >

  </div>
</div>

{{!-- Description !--}}
<div 
  class="tentacle-description mdc-card__actions {{if isExpanded "expanded"}}"
>
  {{#each description as |descLine|}}
    <div class="line">
      {{descLine}}
    </div>
  {{/each}}
</div>

{{!-- Description Toogle !--}}
<div class="mdc-card__actions">
  <button {{action "toogleExpanded"}}
    class="expand mdc-button mdc-card__action mdc-card__action--button"
    title="More options" 
  >
    {{if isExpanded "COLLAPSE" "EXPAND"}}
  </button>
</div>